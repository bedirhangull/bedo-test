services:
  - type: web
    name: bedo-test-mcp
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3100
      - key: MCP_BASE_URL
        sync: false
      - key: HIREG_DASHBOARD_URL
        sync: false
